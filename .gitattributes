# Auto detect text files and normalize line endings to LF in repository
* text=auto

# Python files should always use LF
*.py text eol=lf

# Markdown files should always use LF
*.md text eol=lf

# INI files should always use LF
*.ini text eol=lf

# Shell scripts should always use LF
*.sh text eol=lf

# Windows batch files should use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files
*.parquet binary
*.csv binary
*.pkl binary
*.joblib binary
